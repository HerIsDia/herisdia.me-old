const config = {
  content: ['./src/**/*.{html,js,svelte,ts}', './index.html'],

  theme: {
    extend: {
      fontFamily: {
        mundial: ['Mundial', 'sans-serif'],
      },
    },
  },

  plugins: [],
};

module.exports = config;
